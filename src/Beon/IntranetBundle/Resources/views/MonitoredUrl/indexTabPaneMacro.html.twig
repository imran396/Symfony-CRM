{% macro pane(entities,paneid,paneclass) %}

    <div class="tab-pane {{ paneclass }}" id="{{ paneid }}">
        {% include "IntranetBundle:MonitoredUrl:indexTable.html.twig" %}
    </div>

{% endmacro %}